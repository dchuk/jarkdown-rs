# Agent skills

## Issue tracker

Issues live in GitHub Issues on `dchuk/jarkdown-rs`, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

## Triage labels

Canonical five-role vocabulary, label strings match role names verbatim. `wontfix` already exists on the repo; the other four labels will be created on first triage use. See `docs/agents/triage-labels.md`.

## Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
